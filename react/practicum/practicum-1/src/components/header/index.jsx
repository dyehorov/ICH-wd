import HeaderTitle from "../headerTitle"
import HeaderLogo from "../headerLogo"

function Header() {
  return (
    <header>
      <HeaderTitle />
      <HeaderLogo />
    </header>
  )
}

export default Header
